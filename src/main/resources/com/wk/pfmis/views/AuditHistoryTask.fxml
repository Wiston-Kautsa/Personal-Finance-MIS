<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.ScrollPane?>
<?import javafx.scene.control.TextArea?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.VBox?>

<ScrollPane fitToWidth="true" hbarPolicy="NEVER" vbarPolicy="AS_NEEDED"
            xmlns="http://javafx.com/javafx/21"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.wk.pfmis.controllers.AuditHistoryTaskController"
            styleClass="settings-scroll">
    <content>
        <VBox spacing="14" maxWidth="Infinity" styleClass="settings-page maintenance-simple-page audit-history-simple-page">
            <VBox spacing="7" styleClass="maintenance-simple-header">
                <Label fx:id="titleLabel" text="Audit and History" styleClass="section-heading"/>
                <Label fx:id="subtitleLabel" text="Search, review and export read-only audit evidence." wrapText="true" styleClass="settings-status-text"/>
                <Label fx:id="guidanceLabel" text="Audit evidence is read-only and newest records are shown first." wrapText="true" styleClass="data-preview-banner"/>
            </VBox>

            <VBox fx:id="contentContainer" spacing="12" maxWidth="Infinity" styleClass="maintenance-simple-form"/>

            <TextArea fx:id="resultArea" editable="false" wrapText="true" prefRowCount="7" styleClass="ai-answer-area maintenance-result-area"/>

            <HBox spacing="8" alignment="CENTER_LEFT" maxWidth="Infinity" styleClass="maintenance-simple-actions">
                <Button fx:id="supportingActionButton" text="View Details" onAction="#runSupportingAction" minWidth="140" styleClass="secondary-button"/>
                <Region HBox.hgrow="ALWAYS"/>
                <Button fx:id="mainActionButton" text="Export" onAction="#runMainAction" minWidth="180" styleClass="primary-button"/>
            </HBox>

            <padding>
                <Insets top="4" right="4" bottom="12" left="4"/>
            </padding>
        </VBox>
    </content>
</ScrollPane>
